---
description: 
globs: 
alwaysApply: false
---
# 错误处理规范

## 异常捕获原则

1. 在工具层（`tools/`目录）中捕获所有异常：
   - 用户输入验证错误
   - API调用错误
   - 认证错误
   - 网络错误
   - 意外异常

2. 在提供商层（`provider/`目录）中：
   - 捕获所有验证凭据过程中的异常
   - 将异常转换为`ToolProviderCredentialValidationError`
   - 提供明确的错误消息

## 错误消息格式

1. 错误消息应使用统一的格式：
   - 提供商验证错误：`{provider}提供商参数验证失败: {具体错误}`
   - 工具调用错误：`OCR处理异常: {具体错误}`

2. 提供商标识符必须使用英文，不使用中文名称：
   - 使用：`baidu提供商参数验证失败`
   - 不使用：`百度提供商参数验证失败`

## 日志记录

1. 使用Python标准`logging`模块而非`print`语句
2. 日志级别使用标准：
   - `INFO` - 常规操作信息
   - `WARNING` - 不影响功能但需要关注的问题
   - `ERROR` - 导致功能失败的错误
   - `DEBUG` - 调试信息

## 敏感信息处理

1. 日志中不应包含明文凭据信息
2. 使用`_sanitize_credentials`方法脱敏后再记录日志
3. 敏感字段包括但不限于：
   - 包含`secret`、`sk`、`secret_key`、`access_key_secret`等的字段
