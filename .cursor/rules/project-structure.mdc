---
description: 
globs: 
alwaysApply: false
---
# 项目结构和文件命名规则

## 目录结构

项目采用以下目录结构：

- `provider/` - 提供商实现目录，包含与Dify平台集成的服务提供商
  - [provider/ocr.py](mdc:provider/ocr.py) - OCR服务提供商主入口
- `lib/` - 核心库和实现代码
  - `lib/providers/` - 各OCR服务商的具体实现
  - `lib/core/` - 核心服务抽象和通用功能
- `tools/` - 平台工具实现
  - OCR工具：[tools/ocr_baidu.py](mdc:tools/ocr_baidu.py), [tools/ocr_huawei.py](mdc:tools/ocr_huawei.py), [tools/ocr_aliyun.py](mdc:tools/ocr_aliyun.py)
  - 配置文件：对应的yaml配置文件
- `cache/` - 缓存目录，存储临时文件（如百度OCR token）
  - 所有缓存文件不应提交到版本控制系统

## 文件命名规则

1. **Python模块文件**：使用蛇形命名法（snake_case），如 `ocr_baidu.py`
2. **配置文件**：与对应的实现文件使用相同的基础名称，如 `ocr_baidu.yaml`
3. **提供商标识**：统一使用英文标识符，不使用中文
   - 百度：`baidu`
   - 华为：`huawei`
   - 阿里云：`aliyun`

## JSON响应格式规范

所有工具返回的JSON响应必须遵循以下格式规范：

1. 使用统一的字段名：
   - `success` - 布尔值，表示操作成功或失败
   - `text` - 识别到的文本内容
   - `provider` - 使用的服务提供商（统一使用英文标识符）
   - `raw` - 原始API响应数据（不使用`raw_response`）
   - `error` - 错误信息（仅在失败时出现）

2. 示例成功响应：
```json
{
  "success": true,
  "text": "识别到的文本内容",
  "provider": "baidu", 
  "raw": {...}
}
```

## 缓存管理规则

1. 缓存文件存放在`cache/`目录下
2. 缓存文件不应被提交到版本控制系统
3. 目录结构应保留（通过`.gitkeep`文件）
4. 百度OCR token缓存文件名：`baidu_ocr_token.json`
